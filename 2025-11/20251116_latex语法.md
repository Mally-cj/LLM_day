

# 📌 常用 LaTeX 语法

## 1. **删除线（strike through）**

你常用在回应审稿意见里：

```tex
\sout{这段文字被删除}
```

需要宏包：

```tex
\usepackage{soul}
```

如果是整句话删除，注意英文句子里不要包含数学公式，否则容易出错。

---

## 2. **彩色修改（标红）**

你用得最多的是标红：

```tex
\textcolor{red}{这段是修改内容}
```

宏包：

```tex
\usepackage{color}
```

如果想加背景色：

```tex
\sethlcolor{yellow}
\hl{修改内容}
```

---

## 3. **图（figure）严格控制位置（你一直强调 “不要乱跑”）**

### 最稳定的方式 —— 使用 `\FloatBarrier`

来自 `placeins` 宏包：

```tex
\usepackage[section]{placeins}
```

然后在图之后加：

```tex
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{figures/myfig}
    \caption{说明}
    \label{fig:my}
\end{figure}

\FloatBarrier
```

含义：**在此处强制阻断浮动体，图不会跑到上面去。**

你最近常问的问题：
👉 “我希望图就在这个回复的后面，不要跑走”—就是用 `\FloatBarrier`。

---

## 4. **引用图、表、公式**

你现在写得很熟练，但这里总结最常用：

### 图：

```tex
Fig.~\ref{fig:imp_0}
```

### 表：

```tex
Table~\ref{tab:performance}
```

### 公式：

```tex
Eq.~\eqref{eq:synthetic_record_noise}
```

### 小节

```tex
Section~\ref{sec:method}
```

---

## 5. **引用获得页码（你最近问：`\pageref` 是怎么用的）**

语法：

```tex
见 Page~\pageref{intro_start}
```

**用法：**
你需要在正文中给一个位置加 label，例如：

```tex
\section{Introduction}
\label{intro_start}
```

然后引用它所在页：

```tex
As discussed on Page~\pageref{intro_start}, ...
```

📌 **注意**：`\pageref` 显示 label 所在的页码。
需要编译两次。

---

## 6. **超链接颜色（cite、ref 链接颜色）**

你之前在 PDF 输出中遇到红色边框问题。

推荐的最干净设置：

```tex
\usepackage[hidelinks]{hyperref}
```

如果要显示颜色：

```tex
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
```

---

## 7. **外部文档引用（你常用 `xr` 来引用补充材料）**

你的写法：

```tex
\usepackage{xr}
\externaldocument{manuscript_review1}
```

引用外部文件中的公式：

```tex
Eq.~\eqref{eq:ext_formula}
```

关键技巧：
外部文档必须编译过并生成 `.aux` 文件，才能被引用。

---

## 8. **数学公式常用语法**

### 行内公式

```tex
$r_j = \frac{z_{j+1}-z_j}{z_{j+1}+z_j}$
```

### 行间公式

```tex
\begin{equation}
r_j = \frac{z_{j+1}-z_j}{z_{j+1}+z_j}
\label{eq:reflect}
\end{equation}
```

---

## 9. **伪代码（Algorithm + algpseudocode）**

你论文中算法使用：

```tex
\usepackage{algorithm}
\usepackage{algpseudocode}
```

示例：

```tex
\begin{algorithm}
\caption{Model-Driven Sampling}
\begin{algorithmic}[1]
\State Initialize $z_T \sim \mathcal{N}(0,I)$
\For{$t = T-1$ to $0$}
    \State Compute $\hat{\epsilon}_t$
    \If{$t \equiv 0 \ (\mathrm{mod} \ interval)$}
        \State Apply model-driven optimization
    \EndIf
\EndFor
\end{algorithmic}
\end{algorithm}
```

---

## 10. **让图像浮动不跨页跑（更强控制）**

你也问过 “能否强制图就在这里？”

可以：

```tex
\begin{figure}[H]
```

需要宏包：

```tex
\usepackage{float}
```

但注意 IEEEtran 不建议大量使用。

---

## 11. **公式编号跨章节或自定义**

你论文中常用：

```tex
\begin{equation}
...
\label{eq:model_driven_optimization}
\end{equation}
```

跨文件引用用 `xr`。

---

## 12. **并排子图（subcaption 最稳）**

```tex
\begin{figure}[htbp]
\centering
\begin{subfigure}{0.45\linewidth}
    \includegraphics[width=\linewidth]{a}
    \caption{(a)}
\end{subfigure}
\begin{subfigure}{0.45\linewidth}
    \includegraphics[width=\linewidth]{b}
    \caption{(b)}
\end{subfigure}
\caption{Two subfigures}
\label{fig:ab}
\end{figure}
```

---

# 🎯 总结

* 删除：`\sout{}`
* 标红修改：`\textcolor{red}{...}`
* 图强制位置：`\FloatBarrier`
* 图定位：`H` / `[htbp]`
* 引用图、表、公式：`\ref{}` `\eqref{}`
* 获取页码：`\pageref{}`
* 控制链接颜色：`hyperref`
* 外部文档引用：`xr` / `\externaldocument`
* 子图：`subcaption`
* 伪代码：`algorithm` + `algpseudocode`



